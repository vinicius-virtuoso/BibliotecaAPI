import {
  createBookSchema,
  returnBookListSchema,
  returnBookOneSchema,
} from './book/book.schema'

import { createUserSchema, returnUserSchema } from './user/user.schema'

import { createLoanSchema, returnLoanSchema } from './loan/loan.schema'

import { createCopySchema, returnCopySchema } from './copy/copy.schema'

import { loginSchema, returnLoginSchema } from './login/login.schema'

export {
  createBookSchema,
  createUserSchema,
  createLoanSchema,
  createCopySchema,
  returnUserSchema,
  returnLoanSchema,
  returnCopySchema,
  loginSchema,
  returnLoginSchema,
  returnBookListSchema,
  returnBookOneSchema,
}

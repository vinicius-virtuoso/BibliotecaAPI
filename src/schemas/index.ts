import {
  createBookSchema,
  updateBookSchema,
  returnBookSchema,
} from './book/book.schema'

import {
  createUserSchema,
  updateUserSchema,
  returnUserSchema,
  loginSchema,
  returnLoginSchema,
} from './user/user.schema'

import { createLoanSchema, returnLoanSchema } from './loan/loan.schema'

import { createCopySchema, returnCopySchema } from './copy/copy.schema'

export {
  createBookSchema,
  createUserSchema,
  createLoanSchema,
  createCopySchema,
  updateBookSchema,
  updateUserSchema,
  returnBookSchema,
  returnUserSchema,
  returnLoanSchema,
  returnCopySchema,
  loginSchema,
  returnLoginSchema,
}
